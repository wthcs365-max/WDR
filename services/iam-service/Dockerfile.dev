FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
COPY tsconfig*.json ./
COPY packages/ packages/
COPY services/iam-service/ services/iam-service/
RUN npm install
EXPOSE $(node -e "console.log(require('./services/iam-service/src/config').config.port || 4000)")
CMD ["npx", "ts-node", "--watch", "services/iam-service/src/index.ts"]
