FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
COPY tsconfig*.json ./
COPY packages/ packages/
COPY services/fleet-service/ services/fleet-service/
RUN npm install
EXPOSE $(node -e "console.log(require('./services/fleet-service/src/config').config.port || 4000)")
CMD ["npx", "ts-node", "--watch", "services/fleet-service/src/index.ts"]
