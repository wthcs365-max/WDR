FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
COPY tsconfig*.json ./
COPY packages/ packages/
COPY services/telemetry-service/ services/telemetry-service/
RUN npm install
EXPOSE $(node -e "console.log(require('./services/telemetry-service/src/config').config.port || 4000)")
CMD ["npx", "ts-node", "--watch", "services/telemetry-service/src/index.ts"]
